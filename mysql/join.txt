//1
SELECT  l.LOCATION_ID, l.STREET_ADDRESS, l.CITY, l.STATE_PROVINCE, c.COUNTRY_NAME FROM departments d LEFT JOIN locations l  ON l.LOCATION_ID = d.LOCATION_ID LEFT JOIN countries c ON c.COUNTRY_ID = l.COUNTRY_ID
//2
SELECT e.FIRST_NAME, e.LAST_NAME, e.DEPARTMENT_ID, d.DEPARTMENT_NAME FROM employees e LEFT JOIN departments d ON d.DEPARTMENT_ID = e.DEPARTMENT_ID
//3
SELECT e.FIRST_NAME, e.LAST_NAME, e.JOB_ID, l.COUNTRY_ID FROM employees e RIGHT JOIN departments d ON d.DEPARTMENT_ID = e.DEPARTMENT_ID INNER JOIN locations l ON d.LOCATION_ID = l.LOCATION_ID WHERE COUNTRY_ID = 'UK'
//4
SELECT e1.LAST_NAME, e1.MANAGER_ID, e2.LAST_NAME AS MANAGER_NAME FROM employees e1 INNER JOIN employees e2 ON e1.MANAGER_ID = e2.EMPLOYEE_ID
//5
SELECT e1.FIRST_NAME, e1.LAST_NAME, e1.HIRE_DATE FROM employees e1 INNER JOIN employees e2 ON e1.EMPLOYEE_ID = e2.EMPLOYEE_ID AND e2.HIRE_DATE > (SELECT HIRE_DATE FROM employees WHERE LAST_NAME = 'Higgins')
//6
SELECT d.DEPARTMENT_NAME, tb.COUNT FROM departments d LEFT JOIN (SELECT DEPARTMENT_ID, COUNT(*) AS COUNT FROM employees GROUP BY DEPARTMENT_ID) AS tb ON tb.DEPARTMENT_ID = d.DEPARTMENT_ID
//7
не понял связи между таблицами
//8
не понял связи между таблицами
//9
SELECT j.JOB_ID, e.AVG FROM jobs j LEFT JOIN (SELECT JOB_ID, AVG(SALARY)AS AVG FROM employees GROUP BY JOB_ID) AS e ON j.JOB_ID = e.JOB_ID
//10
SELECT e.JOB_ID, e.FIRST_NAME, e.LAST_NAME, (e.SALARY - j.MIN_SALARY) AS SUBSTRUCTING FROM employees e JOIN jobs j ON e.JOB_ID = j.JOB_ID
//11
SELECT  d.DEPARTMENT_NAME, e2.FIRST_NAME, e2.LAST_NAME, e2.SALARY, e2.HIRE_DATE FROM ( SELECT MANAGER_ID, HIRE_DATE FROM employees GROUP BY MANAGER_ID) AS e1  INNER JOIN employees e2 ON e1.MANAGER_ID = e2.EMPLOYEE_ID AND (YEAR(CURDATE()) - YEAR(e1. HIRE_DATE)) > 15 LEFT JOIN departments d ON e2.DEPARTMENT_ID = d.DEPARTMENT_ID
